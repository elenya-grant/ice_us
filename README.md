# ice_us

Repo for loading, analyzing, and mapping EGridData

```bash
git clone https://github.com/elenya-grant/ice_us.git
cd ice_us


conda create --name ice python=3.11 -y
conda activate ice
conda install geopandas

pip install -e ".[all]"

pre-commit install

```
